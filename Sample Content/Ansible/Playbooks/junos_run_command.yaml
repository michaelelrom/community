---
- connection: network_cli
  gather_facts: no
  hosts: all

  tasks:

    - name: run command
      register: result
      junos_command:
        commands:
          - "{{ command }}"

    - name: Result
      debug: var=result
